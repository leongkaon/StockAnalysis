# -------------------------------------------------------------------------------------------------
library(quantmod);

setSymbolLookup(WK=list(name='0001.hk',src="yahoo", from = as.Date("2000-01-01"), to = Sys.Date()));
getSymbols("WK");
hk_0001 = na.omit(
  data.frame(WK)
)
dim(hk_0001)
head(hk_0001)

# -----------------------------------------------------------------------------------------------






